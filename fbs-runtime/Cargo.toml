[package]
name = "fbs-runtime"
version = "0.1.0"
edition = "2021"

[dependencies]
fbs-executor = { path = "../fbs-executor" }
fbs-reactor = { path = "../fbs-reactor" }
thiserror = "1.0.40"
libc = "0.2.147"
